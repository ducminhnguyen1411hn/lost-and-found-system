@model LostAndFound.Models.ViewModels.Admin.LocationCreateViewModel
@{
    ViewData["Title"] = "Create Location";
}

<div class="container mt-4 mb-5">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card border-0 shadow-sm" style="border-radius: 8px;">
                <div class="card-body p-4">
                    <h4 class="fw-bold mb-4 text-dark">Create New Location</h4>

                    <form asp-action="CreateLocation" method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="Building" class="form-label fw-medium">Building (Optional)</label>
                            <input asp-for="Building" class="form-control" style="border-radius: 6px;" placeholder="e.g., Building A" />
                            <span asp-validation-for="Building" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label fw-medium">Location Name</label>
                            <input asp-for="Name" class="form-control" style="border-radius: 6px;" placeholder="e.g., Library, Room 101" />
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="d-flex gap-2 mt-4">
                            <button type="submit" class="btn btn-primary" style="border-radius: 6px;">Create Location</button>
                            <a asp-action="Locations" class="btn btn-outline-secondary" style="border-radius: 6px;">Cancel</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}