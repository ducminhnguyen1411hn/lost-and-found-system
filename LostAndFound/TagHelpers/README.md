# TagHelpers

Custom ASP.NET Core TagHelpers go here (if any). Built-in TagHelpers (`asp-for`,
`asp-validation-for`, `asp-action`, …) are already enabled in `Views/_ViewImports.cshtml`.
Empty for now.
